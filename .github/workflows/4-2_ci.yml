name: CI
description: Node.jsのプログラムのCI用ワークフロー
on: push
jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: npm install
      - run: cd 4-2_src && npm test
      - run: cd 4-2_src && npm run lint
