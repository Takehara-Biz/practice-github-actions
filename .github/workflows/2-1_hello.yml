name: 2-1 Hello
on: push
defaults:
  run:
    shell: bash
jobs:
  hello:
    runs-on: ubuntu-latest
    timeout-minutes: 1
    steps:
      - run: sleep 5
      - run: echo "Hello, world"
      - uses: actions/checkout@v4
